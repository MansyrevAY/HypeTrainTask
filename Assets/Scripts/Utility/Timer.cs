using System.Timers;

namespace Utility
{
    public class LockTimer
    {
        Timer t = new Timer();
        // Start is called before the first frame update
        void Start()
        {
            
        }

        // Update is called once per frame
        void Update()
        {

        }
    }
}
